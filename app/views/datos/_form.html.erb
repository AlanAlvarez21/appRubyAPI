<%= form_with(model: dato) do |form| %>
  <% if dato.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(dato.errors.count, "error") %> prohibited this dato from being saved:</h2>

      <ul>
        <% dato.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :dato1 %>
    <%= form.text_field :dato1 %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
